<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
        <item name="windowActionBar">false</item>
        <!-- change the light status of bar -->
        <!--<item name="android:windowLightStatusBar">false</item>-->
        <!--remove White screen is displayed while switching between Activities-->
        <item name="android:windowDisablePreview">true</item>

    </style>

    <!--NavigationView Style-->
    <style name="NavigationDrawerTheme">
        <item name="android:textSize">15sp</item><!-- text size in menu-->
        <item name="android:fontFamily">@font/seguisb</item>
    </style>




    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />



    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlActivated">@color/gold</item>
        <item name="colorControlNormal">@color/grayLight2</item>

    </style>

</resources>